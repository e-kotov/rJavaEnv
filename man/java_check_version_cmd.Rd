% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/java_env.R
\name{java_check_version_cmd}
\alias{java_check_version_cmd}
\title{Check installed Java version using terminal commands}
\usage{
java_check_version_cmd(java_home = NULL, quiet = FALSE, .use_cache = FALSE)
}
\arguments{
\item{java_home}{Path to Java home directory. If NULL, the function uses the JAVA_HOME environment variable.}

\item{quiet}{A \code{logical} value indicating whether to suppress messages. Can be \code{TRUE} or \code{FALSE}.}

\item{.use_cache}{Logical. If \code{TRUE}, uses cached results for repeated calls with the same JAVA_HOME. If \code{FALSE} (default), forces a fresh check. Set to \code{TRUE} for performance in loops or repeated checks within the same session.}
}
\value{
A \code{character} vector of length 1 containing the major Java version, or \code{FALSE} if JAVA_HOME is not set or the Java executable cannot be found.
}
\description{
Check installed Java version using terminal commands
}
\section{Performance}{

This function is memoised (cached) within the R session using the effective
JAVA_HOME as cache key. First call for a given JAVA_HOME: ~37ms. Subsequent
calls (with \code{.use_cache = TRUE}): <1ms. When you switch Java versions via \code{use_java()}, JAVA_HOME
changes, creating a new cache entry. Cache is session-scoped.
}

\examples{
\donttest{
java_check_version_cmd()
}

}
