% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/java_check.R
\name{java_check_compatibility}
\alias{java_check_compatibility}
\title{Verify rJava Compatibility (Guard)}
\usage{
java_check_compatibility(
  version,
  type = c("min", "exact"),
  action = c("warn", "stop", "message", "none")
)
}
\arguments{
\item{version}{Integer. The required major Java version (e.g., 21).}

\item{type}{Character. "min" (default) checks for \verb{>= version}. "exact" checks for \verb{== version}.}

\item{action}{Character. What to do if incompatible:
\itemize{
\item "warn": Issue a warning but continue.
\item "stop": Throw an error (prevent package loading).
\item "message": Print a startup message.
\item "none": Return \code{FALSE} invisibly (useful for custom handling).
}}
}
\value{
Logical \code{TRUE} if compatible, \code{FALSE} otherwise.
}
\description{
Checks if the currently initialized \code{rJava} session matches the required Java version.
Intended for use in the \code{.onLoad} function of packages that depend on \code{rJava}.
}
\details{
This function detects if \code{rJava} has already locked to a specific Java version
(which happens upon package loading) and warns the user if that version does not
match the requirement.
}
\examples{
\dontrun{
# In a package .onLoad:
.onLoad <- function(libname, pkgname) {
  rJavaEnv::java_check_compatibility(version = 21, action = "warn")
}
}
}
