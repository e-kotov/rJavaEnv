% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/java_find.R
\name{java_find_system}
\alias{java_find_system}
\title{Discover system-wide Java installations}
\usage{
java_find_system(quiet = TRUE, .use_cache = FALSE)
}
\arguments{
\item{quiet}{Logical. If \code{TRUE}, suppresses informational messages.}

\item{.use_cache}{Logical. If \code{TRUE}, uses memoisation cache for the expensive
system scanning part. The \code{is_default} flag is always calculated dynamically
based on the current \code{JAVA_HOME}. Default: \code{FALSE} (bypass cache for safety).
Set to \code{TRUE} for performance in loops or repeated calls.}
}
\value{
A data frame with columns:
\itemize{
\item \code{java_home}: Character. Path to the Java home directory.
\item \code{version}: Character. Major Java version (e.g., "17", "21").
\item \code{is_default}: Logical. \code{TRUE} if this matches the current system default Java
(determined by \code{JAVA_HOME} or PATH).
Rows are ordered with the default Java first (if detected), then by version descending.
Returns an empty data frame if no Java installations are found.
}
}
\description{
Scans the system for installed Java Development Kits (JDKs).
Implementation based on CMake \code{FindJava.cmake} logic, rJava detection,
and standard OS checks.

\strong{Note}: This function only returns true system installations. Java installations
managed by rJavaEnv (stored in the package cache) are explicitly excluded, even if
\code{JAVA_HOME} or \code{PATH} currently point to them.
}
\section{Performance}{

The system scan is memoised (cached) for the session duration. First scan: ~209ms.
Subsequent scans with \code{.use_cache = TRUE}: <1ms. The \code{is_default} flag is always
calculated fresh to reflect current \code{JAVA_HOME}.
}

\examples{
\dontrun{
# List all system Java installations
java_find_system()

# Use cache for faster repeated calls if you are using it in your R package
java_find_system(.use_cache = TRUE)
}
}
